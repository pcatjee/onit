import React from "react";
import { StyleSheet, View, Text, ImageBackground, require } from "react-native";

const PersonalCare = ({ navigation }) => {
  return (
    <View>
      <Text
        style={{
          fontSize: 30,
          marginTop: 80,
          alignContent: "center",
        }}>
         COMING SOON
      </Text>
    </View>
  );
};

export default PersonalCare;

const style = StyleSheet.create({});
